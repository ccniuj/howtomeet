<%= simple_form_for [:admin, @admin_category] do |f| %>
  <div class="form-group">
    <%= f.input :title, :label => "名稱", :input_html => {:class => "form-control"} %></br>
  </div>
  <%= f.submit "確定", :class => "btn btn-success" %>
<% end %>
