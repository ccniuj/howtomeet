<%= form_for(@admin_meetup) do |f| %>
  <% if @admin_meetup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_meetup.errors.count, "error") %> prohibited this admin_meetup from being saved:</h2>

      <ul>
      <% @admin_meetup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
