<div class="col-md-offset-2 col-md-8">
  <span style="font-size: 18px;">分類</span>
  <%= link_to '全部', find_meetup_path("all"), class: "btn btn-default" %>
  <% @categories.each do |category| %>
    <%= link_to category.title, find_meetup_path(category), class: "btn btn-default" %>
  <% end %>
</div>